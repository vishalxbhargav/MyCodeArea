#include<stdio.h>
#define max=10
int arr[10]={0};
int size=0;
int head=arr[0]=-1;
void addfirst(int n){
    if(arr[o]==-1) arr[0]=n;
    if(size==max)
    for(int i=0;i<max;i++){
        arr[i+1]=arr[i];
    }
}
int main(){
    int arr[2]={1,2};
    int *p=&arr[0];
    *p++;
    printf("%d",*p);
}